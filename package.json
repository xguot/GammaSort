{
  "name": "gammasort",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "data": "node scripts/parse-csv.mjs",
    "dev": "npm run data && astro dev",
    "build": "npm run data && astro build",
    "preview": "astro preview",
    "deploy": "npm run build && npx gh-pages -d dist"
  },
  "dependencies": {
    "@astrojs/tailwind": "^5.1.0",
    "astro": "^4.15.0",
    "csv-parse": "^5.5.0",
    "tailwindcss": "^3.4.0"
  }
}
